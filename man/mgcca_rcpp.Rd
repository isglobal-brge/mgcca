% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{mgcca_rcpp}
\alias{mgcca_rcpp}
\title{MGCCA over HDF5 (single-call C++ orchestrator)}
\usage{
mgcca_rcpp(
  filename,
  in_group,
  datasets,
  nfac,
  inv_method,
  lambda = NULL,
  scores = FALSE,
  scale = TRUE,
  route = "auto",
  tmp_group = "MGCCA_TMP",
  final_group = "FINAL_RESULTS",
  threads = NULL
)
}
\arguments{
\item{filename}{HDF5 file with input tables already written under \code{in_group}.}

\item{in_group}{Input group (default \code{"MGCCA_IN"}).}

\item{datasets}{Character vector of dataset names inside \code{in_group}.}

\item{nfac}{Number of shared components.}

\item{inv_method}{1 = solve (SPD Cholesky), 2 = penalized (+lambda I),
3 = geninv/ginv (pseudoinverse).}

\item{lambda}{Numeric vector (length = #tables) for \code{inv_method = 2}.}

\item{scores}{If TRUE, also compute per-table weights and scores.}

\item{scale}{If TRUE (default), column-center+scale each table in-HDF5
(out-of-core, base-R \code{scale()} semantics) before the analysis, reading
the raw tables from \code{in_group} and never loading them fully into RAM.}

\item{route}{Per-table algebra route: \code{"auto"} (default) picks per table
by \code{min(n, p)} -- covariance \code{X'X} (p x p) when \code{p < n},
Gram \code{XX'} (n x n) when \code{p >= n}; \code{"cov"} forces the
covariance route for all tables; \code{"dual"} forces the Gram route.}

\item{tmp_group}{Group for intermediates (default \code{"MGCCA_TMP"}).}

\item{final_group}{Group for results (default \code{"FINAL_RESULTS"}).}

\item{threads}{Optional thread count.}
}
\value{
A descriptor list (filename, datasets, nfac, m, eigenvalues, route,
  route_dual, final_group). Results are written under \code{final_group}.
}
\description{
MGCCA over HDF5 (single-call C++ orchestrator)
}
\seealso{
\code{\link{mgcca}}, \code{\link{mgcca_results}}
}
\keyword{internal}
