% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{mgcca_rcpp}
\alias{mgcca_rcpp}
\title{MGCCA over HDF5 using BigDataStatMeth (C++ backend)}
\usage{
mgcca_rcpp(
  filename,
  group,
  datasets,
  nfac,
  scale,
  pval,
  scores,
  method,
  lambda,
  mccores
)
}
\arguments{
\item{filename}{String. Path to the HDF5 file.}

\item{group}{String. Input HDF5 group that contains the tables.}

\item{datasets}{Character vector. Dataset names inside \code{group}.}

\item{nfac}{Integer. Number of shared components to extract.}

\item{scale}{Integer. 0 = none, 1 = center, 2 = center+standardize.}

\item{pval}{Integer. Power parameter \code{p} for the GCCA objective.}

\item{scores}{Integer (0/1). If 1, write per-table scores to HDF5.}

\item{method}{String. GCCA scheme (e.g., "horst", "centroid", "factorial").}

\item{lambda}{Numeric. Ridge/shrinkage penalty (applied per table if scalar).}

\item{mccores}{Integer. Number of CPU cores/threads to use.}
}
\value{
Invisibly returns \code{TRUE}; primary outputs are written to HDF5.
}
\description{
Compute Generalized CCA components on multi-omic tables
  stored in an HDF5 file, operating block-wise via BigDataStatMeth.
  Results are written under the group \code{MGCCA_OUT} in the same file.
}
\details{
Inputs are read block-wise from HDF5; large intermediates are
  avoided. Outputs include \code{Y} (shared components), \code{corsY},
  \code{scores/<table>} and AVE metrics inside \code{MGCCA_OUT}.
}
\note{
C++ identifiers cannot contain dots. Implement the argument as
  \code{mc_cores} in C++ and expose/rename to \code{mccores} on the R side
  if you need the dotted name in R.
}
\references{
van de Velden, M. & Takane, Y. (2012). Generalized CCA with
  missing values for multi-block data integration.
}
