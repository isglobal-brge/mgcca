% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plotIndividuals}
\alias{plotIndividuals}
\title{Plot individuals on the shared components}
\usage{
plotIndividuals(
  x,
  group = NULL,
  comps = c(1, 2),
  label = FALSE,
  ellipse = TRUE,
  level = 0.95,
  point_size = 2.2,
  title = "Individuals"
)
}
\arguments{
\item{x}{an \code{mgcca} object (from \code{\link{mgcca_results}} or
\code{mgcca(collect = TRUE)}).}

\item{group}{optional factor to colour individuals by. If it carries names
they are matched to the row names of \code{Y}; otherwise it is assumed to be
in the same order as the individuals.}

\item{comps}{the two components to display. Default \code{c(1, 2)}.}

\item{label}{logical; label points with individual IDs. Default FALSE.}

\item{ellipse}{logical; when a \code{group} is given, overlay a 95\% confidence
ellipse per group (groups with fewer than 4 individuals, and the \code{NA}
group, are skipped). Makes the separation legible when points overlap.
Default TRUE.}

\item{level}{confidence level for the ellipses. Default 0.95.}

\item{point_size}{point size. Default 2.2.}

\item{title}{plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Scatter plot of the shared canonical components \code{Y} (one
  point per individual), optionally coloured by a grouping factor. A modern,
  colourblind-safe replacement for \code{\link{plotInds}} that returns a
  \code{ggplot} object you can further customise.
}
\seealso{
\code{\link{plotVariables}}, \code{\link{plotAVE}}
}
