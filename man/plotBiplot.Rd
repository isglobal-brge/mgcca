% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plotBiplot}
\alias{plotBiplot}
\title{Biplot: individuals and variable loadings together}
\usage{
plotBiplot(
  x,
  table = 1,
  comps = c(1, 2),
  top = 8,
  group = NULL,
  title = "Biplot"
)
}
\arguments{
\item{x}{an \code{mgcca} object.}

\item{table}{table name or index into \code{x$corsY} for the variable arrows.
Default 1.}

\item{comps}{the two components to display. Default \code{c(1, 2)}.}

\item{top}{number of variables (by correlation radius) to draw. Default 8.}

\item{group}{optional factor to colour individuals by.}

\item{title}{plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Overlays the individuals (shared components \code{Y}) and, for one
  table, the loadings of its top variables as arrows -- a compact view of which
  variables pull the individuals where. Loadings are scaled to the span of the
  individual cloud for readability.
}
\examples{
data(cardiovascular)
u   <- Reduce(union, list(rownames(X1), rownames(X2), rownames(X3)))
sel <- u[1:150]
mk  <- function(d, cols = NULL) {
    m <- as.matrix(d[rownames(d) \%in\% sel, , drop = FALSE])
    if (!is.null(cols)) m <- m[, cols, drop = FALSE]
    storage.mode(m) <- "double"
    m
}
X <- list(methylation = mk(X1, 1:20), clinical = mk(X2), other = mk(X3))
fit <- mgcca(X, nfac = 2, method = "penalized", lambda = rep(0.1, 3))

plotBiplot(fit, table = "clinical", top = 4)
}
\seealso{
\code{\link{plotIndividuals}}, \code{\link{plotVariables}}
}
