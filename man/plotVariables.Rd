% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plotVariables}
\alias{plotVariables}
\title{Correlation circle of variables for one table}
\usage{
plotVariables(x, table = 1, comps = c(1, 2), top = 10, title = NULL)
}
\arguments{
\item{x}{an \code{mgcca} object.}

\item{table}{table to display: a name or an index into \code{x$corsY}.
Default 1.}

\item{comps}{the two components to display. Default \code{c(1, 2)}.}

\item{top}{number of top variables (by correlation radius) to draw as
labelled arrows. Default 10.}

\item{title}{plot title. Default: the table name.}
}
\value{
A \code{ggplot} object.
}
\description{
For a chosen table, draws the correlation of each variable with
  the two shared components as a correlation circle (arrows within the unit
  circle). Only the \code{top} variables with the largest correlation radius
  are drawn as labelled arrows, so the plot stays readable even for
  omics-scale tables; the rest are shown as faint points.
}
\examples{
data(cardiovascular)
u   <- Reduce(union, list(rownames(X1), rownames(X2), rownames(X3)))
sel <- u[1:150]
mk  <- function(d, cols = NULL) {
    m <- as.matrix(d[rownames(d) \%in\% sel, , drop = FALSE])
    if (!is.null(cols)) m <- m[, cols, drop = FALSE]
    storage.mode(m) <- "double"
    m
}
X <- list(methylation = mk(X1, 1:20), clinical = mk(X2), other = mk(X3))
fit <- mgcca(X, nfac = 2, method = "penalized", lambda = rep(0.1, 3))

## the clinical table has few variables: label them all
plotVariables(fit, table = "clinical", top = 6)

## on an omics-scale table only the leading variables are labelled
plotVariables(fit, table = 1, top = 5)
}
\seealso{
\code{\link{plotIndividuals}}, \code{\link{getSignif}}
}
